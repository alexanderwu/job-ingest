# For non-uv users; both scripts also carry PEP 723 headers (`uv run <script>`).
# ingest_and_benchmark.py:
duckdb>=1.0
pyarrow>=17
msgspec>=0.18
# verify_parity.py needs only msgspec + pyarrow from the above.
